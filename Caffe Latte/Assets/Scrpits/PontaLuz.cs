using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PontaLuz : MonoBehaviour
{

    //public Transform PlayerPosition;
    //public Transform TrianguloLuz;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 mousePosition =  Camera.main.ScreenToWorldPoint(Input.mousePosition);



       // if (Vector2.Distance(mousePosition, PlayerPosition.position) < 6f)
        //{
       //     transform.position = new Vector3(mousePosition.x, mousePosition.y, -1);
       // }

       // float distanciaPlayerCirculo = Vector2.Distance(transform.position, PlayerPosition.position);
        //TrianguloLuz.localScale = new Vector3(22f, distanciaPlayerCirculo*5.2f, 1f);
        //TrianguloLuz.position = new Vector3();
       
    }
}
